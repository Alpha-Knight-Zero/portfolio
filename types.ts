import { IconType } from 'react-icons';

export interface IDomains {
	title: string;
	description: string;
	Icon: IconType;
}

export interface ISkill {
	title: string;
	level: string;
	Icon: IconType;
}
